.tag_list.well
  = link_to "SORT BY TAGS", "#", class: "btn btn-info sort_by_tags"
  .primary_tags
    - @muscles.each do |tag|
      = link_to_remove_tag tag, :muscles

  .muscle_tags.hidden.tags_space
    Muscles:
    - @muscles.each do |tag|
      = link_to_tag tag, :muscles
      = link_to_remove_tag tag, :muscles
      |

/ %p
/   Equipment:
/   - @equipment.each do |tag|
/     = link_to_tag tag, :equipment
/     = link_to_remove_tag tag, :equipment
/     |