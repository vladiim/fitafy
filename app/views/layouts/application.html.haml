!!! 5
-# http://paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither
- html_tag :class => "no-js", :lang => "en" do
  = render "head"

  %body#main.container{ :class => "#{ controller.controller_name }" }
    = render "chromeframe"
    .main_section= render "header"

    %nav
      = render "layouts/navigation"

    %section.main_section
      = render "layouts/flashes"

    %section.main_section.container{ :role => "main" }
      = yield

    = render "footer"

    - if can? :manage, Workout
      = render partial: "workouts/new_from_nav"

    = javascript_include_tag "application"

= debug(params) if Rails.env.development?