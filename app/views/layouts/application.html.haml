!!! 5
-# http://paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither
- html_tag :class => "no-js", :lang => "en", "ng-app" => "Fitafy" do
  = render "head"

  %body{ :class => "#{ controller.controller_name } container-fluid" }
    %noscript You need to enable Javascript to get the most out of fitafy

    %a{ :accesskey => 's', :href => '#main', class: 'hidden' } skip to content
    = render "chromeframe"

    %nav= render "layouts/navigation"

    %section#main{ :role => "main" }
      = render "layouts/flashes"

      = render "header"

      = yield

    = render "footer"

    = javascript_include_tag "application"

= debug(params) if Rails.env.development?