$(".edit_workout_form").on("ajax:success", function() {
  $("li.workout_exercise").remove();
  $('ul.workout_exercises').append('<%= j render partial: "workouts/show_exercise", collection: @workout.workout_exercises, as: :exercise %>');
});