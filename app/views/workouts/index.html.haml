%section#workout_filters
  = render partial: "tags/list", locals: { width: 9 }

%section#workouts.row
  %ul#workout_list{"data-json-url" => workouts_url(format: :json)}
    - @workouts.each do |workout|
      - @renderer.workout = workout
      = @renderer.render.html_safe

.end_of_workouts
  %p