%li#box_list.well.span2
  %article
    %header
      %h5= link_to workout.name.titleize, user_workout_path(workout.user_id, workout)

    %p
      = workout.client_level.upcase
      = workout.difficulty.upcase

    %p
      By: CLUB_RYNO

    %p
      = workout.exercises_count
      EXERCISES